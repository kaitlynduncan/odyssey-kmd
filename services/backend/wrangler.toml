name = "odyssey-backend"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Set DATABASE_URL locally via .dev.vars (gitignored):
#   DATABASE_URL=postgres://...
# and in production via `wrangler secret put DATABASE_URL`.
